import { Head } from '@inertiajs/react'; import { useMemo,useState } from 'react'; import { Card,CardContent,CardHeader,CardTitle } from '@/components/ui/card'; import { Toolbar,Empty } from '@/components/workspace-ui'; import { AppLayout } from '@/layouts/app-layout';
export default function Help({faqs}:{faqs:Array<{question:string;answer:string}>}){const[q,setQ]=useState('');const list=useMemo(()=>faqs.filter(x=>(x.question+' '+x.answer).toLowerCase().includes(q.toLowerCase())),[q,faqs]);return <><Head title="Help center"/><AppLayout title="Help center" description="Guidance for using your community garden workspace." actions={<div className="w-full sm:w-72"><input className="h-9 w-full rounded-lg border bg-card px-3 text-sm outline-none" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search help" aria-label="Search help"/></div>}>{list.length===0?<Empty message="No help topics match your search."/>:<div className="grid gap-4 md:grid-cols-2">{list.map(x=><Card key={x.question}><CardHeader><CardTitle className="text-base">{x.question}</CardTitle></CardHeader><CardContent className="text-sm leading-6 text-muted-foreground">{x.answer}</CardContent></Card>)}</div>}</AppLayout></>}
