import { Head, router, useForm } from '@inertiajs/react'; import { useState } from 'react'; import { Button } from '@/components/ui/button'; import { Card } from '@/components/ui/card'; import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from '@/components/ui/table'; import { Empty,Field,FilterSelect,FormDialog,Pagination,type Paginated,StatusBadge,Toolbar,fieldClass } from '@/components/workspace-ui'; import { AppLayout } from '@/layouts/app-layout';
type Member={id:number;name:string;email:string;role:'member'|'staff';is_active:boolean;plot_requests_count:number;plot_assignments_count:number};
export default function Members({members,filters}:{members:Paginated<Member>;filters:{search?:string;role?:string;status?:string}}){const[selected,setSelected]=useState<Member|null>(null);const form=useForm({role:'member',is_active:true});const edit=(m:Member)=>{setSelected(m);form.setData({role:m.role,is_active:m.is_active})};return <><Head title="Members"/><AppLayout title="Members" description="Manage community access and operational roles." actions={<Toolbar path="/members" search={filters.search} filters={<><FilterSelect label="Role" value={filters.role} options={[["member","Member"],["staff","Staff"]]} onChange={role=>router.get('/members',{...filters,role},{preserveState:true})}/><FilterSelect label="Status" value={filters.status} options={[["active","Active"],["suspended","Suspended"]]} onChange={status=>router.get('/members',{...filters,status},{preserveState:true})}/></>}/>}>{members.data.length===0?<Empty message="No members found."/>:<Card className="overflow-hidden"><Table><TableHeader><TableRow><TableHead>Person</TableHead><TableHead>Role</TableHead><TableHead>Status</TableHead><TableHead>Requests</TableHead><TableHead>Assignments</TableHead><TableHead className="text-right">Actions</TableHead></TableRow></TableHeader><TableBody>{members.data.map(m=><TableRow key={m.id}><TableCell className="font-medium">{m.name}<span className="block text-xs font-normal text-muted-foreground">{m.email}</span></TableCell><TableCell className="capitalize">{m.role}</TableCell><TableCell><StatusBadge value={m.is_active?'active':'suspended'}/></TableCell><TableCell>{m.plot_requests_count}</TableCell><TableCell>{m.plot_assignments_count}</TableCell><TableCell className="text-right"><Button size="sm" variant="outline" onClick={()=>edit(m)}>Manage</Button></TableCell></TableRow>)}</TableBody></Table><Pagination page={members}/></Card>}</AppLayout><FormDialog open={!!selected} onOpenChange={v=>!v&&setSelected(null)} title="Manage account" description={selected?`Update access for ${selected.name}.`:''} processing={form.processing} onSubmit={()=>selected&&form.put(`/members/${selected.id}`,{onSuccess:()=>setSelected(null)})}><Field label="Role"><select className={fieldClass} value={form.data.role} onChange={e=>form.setData('role',e.target.value)}><option value="member">Member</option><option value="staff">Staff</option></select></Field><label className="flex items-center gap-3 rounded-lg border p-3 text-sm"><input type="checkbox" checked={form.data.is_active} onChange={e=>form.setData('is_active',e.target.checked)}/> Account is active</label></FormDialog></>}
