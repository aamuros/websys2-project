import { Head,Link } from '@inertiajs/react'; import { Button } from '@/components/ui/button'; import { Card,CardContent,CardHeader,CardTitle } from '@/components/ui/card'; import { AppLayout } from '@/layouts/app-layout';
export function OperationsDashboard({admin=false,metrics,requests}:{admin?:boolean;metrics:Record<string,number>;requests:any[]}){const title=admin?'Admin dashboard':'Staff dashboard';return <><Head title={title}/><AppLayout title={title} description={admin?'Manage access and oversee garden operations.':'Review requests and coordinate garden operations.'} actions={<Button asChild><Link href="/plot-requests">Review requests</Link></Button>}><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{Object.entries(metrics).map(([key,value])=><Card key={key}><CardHeader className="pb-2"><CardTitle className="text-sm capitalize text-muted-foreground">{key}</CardTitle></CardHeader><CardContent className="text-3xl font-semibold">{value}</CardContent></Card>)}</div><Card className="mt-5"><CardHeader><CardTitle>Pending requests</CardTitle></CardHeader><CardContent className="space-y-3">{requests.length?requests.map(r=><div key={r.id} className="flex items-center justify-between gap-4 rounded-lg border p-3"><div><p className="font-medium">{r.user.name}</p><p className="text-sm text-muted-foreground">Plot {r.garden_plot?.plot_code??'not selected'}</p></div><Button size="sm" asChild><Link href="/plot-requests">Review</Link></Button></div>):<p className="text-sm text-muted-foreground">No pending requests.</p>}</CardContent></Card></AppLayout></>}
